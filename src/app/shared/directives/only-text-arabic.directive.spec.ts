import { OnlyTextArabicDirective } from './only-text-arabic.directive';

describe('OnlyTextArabicDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyTextArabicDirective();
    expect(directive).toBeTruthy();
  });
});
